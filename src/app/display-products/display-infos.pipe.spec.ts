import { DisplayInfosPipe } from './display-infos.pipe';

describe('DisplayInfosPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayInfosPipe();
    expect(pipe).toBeTruthy();
  });
});
